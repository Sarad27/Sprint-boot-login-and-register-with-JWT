server:
  port: 8000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/human_resource_management
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

